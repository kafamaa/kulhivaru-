import { SuperAdminPlaceholder } from "@/src/features/superadmin/components/superadmin-placeholder";

export default function Page() {
  return (
    <SuperAdminPlaceholder
      title="Layout"
      description="Structural layout presets — spec §22. (Route: /superadmin/layout — sibling to layout.tsx, valid in App Router.)"
    />
  );
}

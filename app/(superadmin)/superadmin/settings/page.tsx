import { SuperAdminPlaceholder } from "@/src/features/superadmin/components/superadmin-placeholder";

export default function Page() {
  return (
    <SuperAdminPlaceholder
      title="Platform settings"
      description="Timezone, security policy, retention — spec §27. Uses platform_settings keys."
    />
  );
}
